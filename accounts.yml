accounts:
  - username: placeholder
    token: ghp_REPLACE_ME
    runner_repo: wvm-runner
    max_concurrent: 5

server:
  host: 0.0.0.0
  port: 8765
  api_token: "change-me-secret"
  # ws_url: "wss://your-domain.com"  # agent 反连地址，有反代时配置；默认 ws://host:port
